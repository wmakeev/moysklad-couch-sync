module.exports = function nonJsonContentsRemove (next) {
  return value => {
    if (value.contents && (!value.filename || value.filename.slice(-5) !== '.json')) {
      delete value.contents
    }
    next(value)
  }
}
